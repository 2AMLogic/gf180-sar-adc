# ERC record 20260923-084733-5f13cf9

Append-only evidence record for the `klt erc` structural power-delivery flow (`layout/erc/`). Generated by `layout/erc/run_erc.py`; never edited in place -- a re-run mints a new record.

- **Record ID** — `20260923-084733-5f13cf9`
- **Claim** — every supply `layout/erc/adc_block.supply-spec.json` declares resolves to exactly one electrical island in `layout/adc-top/adc_block.gds`: zero `erc.unconnected_net`, zero `erc.supply_short`. **And** every one of the block's 25 drawn `Nwell` islands carries an `Nplus`-marked tap that reaches `vdd`: zero `erc.missing_tie`, from a rule that was **checked**, not skipped (`erc_coverage.checked` carries `erc.missing_tie:["nwell_tap"]`, asserted by `cases.json`).
- **Why that zero is not vacuous** — a check that reports nothing is evidence only if it could have reported something. `adc_block.tie-wrong-implant.control` below is the same tie declaration narrowed on `Pplus` 31/0 instead of `Nplus` 32/0 -- a non-empty implant this stream really draws, on the substrate ties outside every well -- and it reports all 25 wells untapped. Issue #356; `spec/decision-records/DR-0035-well-taps-and-tie-straps.md`.
- **Not claimed** — the substrate tie. `klt erc`'s `ties[]` needs a drawn `well_layer` and the p-substrate is not drawn, so the two `Pplus`-marked guard rings are graded here only as part of the `vss` island (they carry `vss` labels and are routed into it, which is what zero `erc.unconnected_net` on `vss` asserts) -- not by `erc.missing_tie`. `layout/erc/well_tap_audit.py` measures the rings' geometry directly.
- **Cross-checked from geometry** — `layout/erc/well_tap_audit.py` (`layout/erc/well-tap-audit.json`) re-derives the same answer from the drawn layers alone, with no extraction deck and no net model: 25 well-tap candidates in 25 wells, and both substrate-tie rings closed and carrying `Metal1` labels.
- **Geometry** — `layout/adc-top/adc_block.gds` (sha256 `501f39853a918d6566fd3856e53722410aaaade0d1d1b3f7fb7ae9394ba4d748`), top cell `ADC_BLOCK`.
- **Overall** — PASS (every case reported exactly what it was expected to)

## Toolchain

| Component | Value |
|---|---|
| `klt_version` | `0.5.0+g67d617f899c7` |
| `klt_git_commit` | `67d617f899c7fb941b86cfc85491f75b91ae1176` |
| `klt_install` | `git+https://github.com/2AMLogic/klayout-tools@67d617f899c7fb941b86cfc85491f75b91ae1176` |
| `klt_path` | `/tmp/kltvenvs/erc/bin/klt` |
| `klayout_package` | `0.30.10` |
| `python` | `3.12.3` |
| `platform` | `Linux x86_64` |
| `repo_git_sha` | `5f13cf97d79841b4962470df5b8dee7812f802ee` |
| `repo_dirty` | `True` |

## Result

| Case | Role | `status` | `erc_status` | findings | `klt erc` exit | Match |
|---|---|---|---|---|---|---|
| `adc_block.supply` | item-11 supply spec (the artifact) | `not_checked` | `clean` | `{}` | 4 | yes |
| `adc_block.unconnected-net-control` | negative control | `violations` | `violations` | `{"erc.unconnected_net": 1}` | 3 | yes |
| `adc_block.tie-bare.known-gap` | known gap (A) | `not_checked` | `clean_partial` | `{}` | 4 | yes |
| `adc_block.tie-wrong-implant.control` | discrimination control for ties[] | `violations` | `violations` | `{"erc.missing_tie": 25}` | 3 | yes |
| `adc_block.no-devices.known-gap` | known gap (C) | `not_checked` | `clean` | `{}` | 4 | yes |

`klt erc` exit codes: `0` clean/clean_partial, `3` violations, `4` no antenna level could be graded (`status: "not_checked"`). Exit `4` is the expected code for **every** gf180mcu run: `klt erc` ships an antenna-ratio table for sky130 only, so no level on this PDK is ever compared against a limit. That is a statement about the antenna half of the envelope and says nothing about the connectivity half, whose verdict is `erc_status`. Item 11 grades the supply-continuity rules directly, not this `status`.

## What the supply case actually says

- `erc_status`: `clean` — no connectivity rule fired and no connectivity work was skipped.
- `gate_count`: 142 gate nets, identified as `poly ∩ diff` (`stackup[0].active_layer`).
- `vdd`: one island, carrying all four `vdd` labels (`ADC_DECODE_BANK_N`, `ADC_DECODE_BANK_P`, `ADC_TOP_SW`, `COMPARATOR`) — and, since #356, the 25 n-well taps routed into it.
- `vss`: one island, carrying all six `vss` labels — the same four sub-blocks plus both `Pplus`-marked substrate-tie guard rings — and **not** the same island as `vdd`.
- `erc.missing_tie`: zero, over 25 wells, with the rule in `erc_coverage.checked`.

## Artifacts

| Case | Spec | JSON report | text report |
|---|---|---|---|
| `adc_block.supply` | `layout/erc/adc_block.supply-spec.json` | `reports/20260923-084733-5f13cf9/adc_block.supply.erc.json` | `reports/20260923-084733-5f13cf9/adc_block.supply.erc.txt` |
| `adc_block.unconnected-net-control` | `layout/erc/controls/adc_block.unconnected-net-control.json` | `reports/20260923-084733-5f13cf9/adc_block.unconnected-net-control.erc.json` | `reports/20260923-084733-5f13cf9/adc_block.unconnected-net-control.erc.txt` |
| `adc_block.tie-bare.known-gap` | `layout/erc/controls/adc_block.tie-bare.known-gap.json` | `reports/20260923-084733-5f13cf9/adc_block.tie-bare.known-gap.erc.json` | `reports/20260923-084733-5f13cf9/adc_block.tie-bare.known-gap.erc.txt` |
| `adc_block.tie-wrong-implant.control` | `layout/erc/controls/adc_block.tie-wrong-implant.control.json` | `reports/20260923-084733-5f13cf9/adc_block.tie-wrong-implant.control.erc.json` | `reports/20260923-084733-5f13cf9/adc_block.tie-wrong-implant.control.erc.txt` |
| `adc_block.no-devices.known-gap` | `layout/erc/controls/adc_block.no-devices.known-gap.json` | `reports/20260923-084733-5f13cf9/adc_block.no-devices.known-gap.erc.json` | `reports/20260923-084733-5f13cf9/adc_block.no-devices.known-gap.erc.txt` |

## Reproduce

```bash
pip install 'git+https://github.com/2AMLogic/klayout-tools@67d617f899c7fb941b86cfc85491f75b91ae1176'
python3 layout/erc/run_erc.py --check    # re-run and assert
python3 layout/erc/run_erc.py --verify   # re-read this record, no klt
```

The JSON report is the stable contract; the `.txt` capture beside it is a courtesy view, not the source of truth. Each committed JSON carries one added key, `_klt_exit_code`, which is this runner's record of the process exit status rather than part of `klt erc`'s own envelope — `--verify` strips it before asserting.

- **Timestamp** — 2026-09-23T08:47:33+0000
- **Author** — Loom Builder agent (issue #378)
