# design/sar-logic/rtl — the sign-off RTL, and the library choice DR-0023 deferred

DR-0023 (`../../spec/decision-records/DR-0023-digital-interface-device-flavor.md`)
resolved the blocking device-flavor conflict `../README.md`'s "What rung 1
does and does not establish" section records: the open gf180mcu PDK ships no
3.3 V-device standard-cell library, so DR-0023 adopted the PDK-shipped
`gf180mcu_fd_sc_mcu7t5v0` / `gf180mcu_fd_sc_mcu9t5v0` libraries (both built
from 6 V-oxide devices, both characterized at the block's 3.3 V corner
family) for the digital partition instead. DR-0023 explicitly left **which**
of the two libraries this design is actually built against to a follow-on
issue — this directory, and issue #272, is that follow-on.

```
design/sar-logic/rtl/
  sar_ctrl.v   the sign-off RTL -- hand-authored, port-identical to
               sar_ctrl_a (design/sar-logic/sar_ctrl.spice)
  README.md    this file
```

## `sar_ctrl.v` vs. the rung-1 model: what changed, what didn't

`design/sar-logic/sar_ctrl.spice`'s `sar_ctrl_a` (generated by
`../gen_sar_logic.py`) remains **the executable specification** — DR-0010's
"From rung 1 to the sign-off netlist" is authoritative that the rung-1 model
is not the netlist that tapes out, and `../README.md`'s own "Path to the
netlist layout and LVS will use" section says the same. `sar_ctrl.v`
implements exactly the same behaviour (DR-0008's synchronous one-hot 16-phase
ring, DR-0014's two-phase bottom-plate sample, the free-MSB trial, the nine
`eng`/`dir` weight slices, the four-leg one-hot decode, the `ph14`/`ph15`
output-register load and `drdy`) against the **identical, verbatim** port
list `_ports_ctrl_analog()` in `gen_sar_logic.py` emits for `sar_ctrl_a`: same
71 scalar nets, same order, same names. That port compatibility is required,
not decorative — it is what lets a later step swap this module (or its
synthesized gate netlist) into `sim/sar-logic-functional/` /
`sim/sar-logic-timing/`'s existing testbenches unedited. That gate-level
functional/timing corner-grid replay (`sim/sar-logic-functional-gates/` /
`sim/sar-logic-timing-gates/`) is the scope of #273, a follow-on issue this
PR files, not of this PR itself — see "Verification performed" below for
exactly what is and is not proven here.

One structural choice is deliberate and worth stating up front: `sar_ctrl.v`
is a **single flat module**, with no submodule instantiation and no
generate-block register arrays, even though the rung-1 model itself is built
from three small subckts (`sar_bitreg`, `sar_slice`, `sar_seq`) and this file
inlines all three at the signal-equation level instead of mirroring that
hierarchy in Verilog. Two reasons, both load-bearing for what this issue
needs to prove:

1. `klt equiv`'s `"yosys-sequential"` engine (register-correspondence
   sequential equivalence, klayout-tools#1313) pairs `gold`/`gate` state
   elements **by name** (`equiv_make`'s own matching rule — see
   `docs/cli/equiv.md` in klayout-tools). A flat module free of hierarchy or
   synthesizer-introduced dotted/bit-blasted names keeps every register name
   (`ph0`..`ph15`, `eng9`..`eng1`, `q9`..`q0`, `c9_r`..`c0_r`) identical
   before and after `klt synthesize`'s `synth`/`dfflibmap`/`abc` passes,
   which is what makes that by-name pairing possible at all. It worked:
   both synthesis records below report `klt equiv` `"equivalent"` with zero
   manual `port_map`/register-remap fiddling.
2. It mirrors the rung-1 model's own choice not to build `sar_ctrl_a` out of
   deeply nested subckts beyond `sar_bitreg`/`sar_slice`/`sar_seq` — this
   file inlines those three at the signal-equation level for the same
   reason: a reader comparing this file against `gen_sar_logic.py`'s
   `library()` function and `../README.md`'s narrative is reading the same
   design twice under the same vocabulary, not two designs that happen to
   agree.

## Verification performed

- **Synthesis**: `python3 ../flow/synth_sar_ctrl.py` drives `klt synthesize`
  (Yosys + bundled ABC) against both libraries at the `tt_025C_3v30` corner
  DR-0023 names for the digital partition, with no ABC delay target
  (`constraints.clock_period_ns: null` — timing closure is follow-on (c),
  #275, out of scope here). Both runs are fully mapped (zero non-standard-cell
  instances, checked independently of `klt synthesize`'s own check) — see
  `../flow/sar_ctrl/records/`.
- **Equivalence**: `klt equiv` (`"yosys-sequential"` engine) between
  `sar_ctrl.v` and each synthesized netlist reports `"equivalent"` — every
  one of the 45 flip-flops and every output is proven register-correspondent
  between the RTL and both gate netlists. This is the load-bearing
  correctness claim for this issue: it is what lets the acceptance criteria
  say "the RTL is what the gate netlist implements" rather than merely
  "the RTL compiles".
- **Functional/timing replay over the real corner grid — scaffolded (#273),
  the ngspice convergence blocker fixed (#282), the corner-grid run itself
  still pending (#289).** `../flow/gate_netlist_to_spice.py` translates a
  synthesized gate netlist into a flat SPICE `.subckt` wired against the
  PDK's own standard-cell `.SUBCKT` pin order; `../flow/
  gen_sar_ctrl_gates_tb.py` uses it to build `sim/sar-logic-functional-gates/`
  / `sim/sar-logic-timing-gates/` — the gate-level SPICE replay of
  `../../sim/sar-logic-functional/` / `../../sim/sar-logic-timing/`'s
  closed loops with the *chosen* library's netlist wrapped in for the
  ideal-XSPICE `sar_ctrl_a`, including the `start` seed pulse this
  directory's "Real hardware note" requires. The first attempt to run the
  corner grid surfaced a genuine ngspice convergence failure for the full
  ~181-cell synthesized DUT (a combinational node whose value is provably
  unique given the applied stimulus settled near 0 V instead of VDD).
  **Root cause (#282), found by bisection to a full `.op` node dump rather
  than by inspection: the supply net every standard cell binds
  (`vdd_gate`) is supplied from OUTSIDE the emitted `.subckt sar_ctrl_a`,
  and nothing inside it drives that net — SPICE scopes every node inside a
  `.subckt` LOCALLY to each instance unless it is a port, node `0`, or
  declared `.global`. Without a `.global vdd_gate` declaration, every one
  of the DUT's 181 cell instances got its own private, undriven copy of the
  supply, and the whole network simulated unpowered at ~0 V — a wiring-
  scope bug, not a solver/power-up-state problem (a slow VDD ramp, tried
  first, changed nothing, for exactly this reason).** `gate_netlist_to_spice.py`
  now emits the required `.global` declaration for every externally-supplied
  net a translated subckt binds, guarded by a structural check
  (`_assert_external_nets_reachable`) that refuses to emit a subckt where
  such a net would be left locally scoped; `gen_sar_ctrl_gates_tb.py`
  re-checks the same property on the fully assembled deck. The fix was
  validated on a small, standalone, non-closed-loop probe deck (driving
  `sar_ctrl_a` directly with no CDAC loop) before being re-run against the
  closed-loop decks: the previously-degenerate node now reads exactly VDD,
  and the DUT's internal `ph` ring correctly reaches its one-hot state,
  confirming a trustworthy digital operating point. What #282 did **not**
  complete: actually executing `sim/run_corners.py` over the full ratified
  `mos` grid for either deck and recording a PASS/FAIL result — both decks'
  own `tb.json` already document a substantial compute cost for that (on
  the order of hours of serial CPU-time per deck even before #282's session
  additionally hit severe host contention), so that execution/recording
  step is tracked separately as issue #289 rather than block on it here.
  Both testbenches, their `tb.json` manifests and
  `gate_netlist_to_spice.py`'s own regression tests are committed; no
  `sim/sar-logic-functional-gates/records/` or
  `sim/sar-logic-timing-gates/records/` exist yet — #289 tracks producing
  them (PASS or FAIL, per `CLAUDE.md` — a genuine gate-level regression is
  recorded, not tightened away).

## Library choice: `gf180mcu_fd_sc_mcu7t5v0`

Both libraries synthesize cleanly and both are proven equivalent to the RTL
(see the two records under `../flow/sar_ctrl/records/`), so the choice comes
down to area against `layout/adc-top/README.md`'s §2.4 reserved SAR-logic
footprint — the thing DR-0023 explicitly named as this issue's own deciding
factor ("compare against `layout/adc-top/README.md`'s §2.4 reserved
sequencer footprint").

| Library | Cell instances | `area_um2` (Liberty, cell area only) | Sequential area |
|---|---|---|---|
| `gf180mcu_fd_sc_mcu7t5v0` (7-track) | 181 | **4,961.152** | 2,864.736 |
| `gf180mcu_fd_sc_mcu9t5v0` (9-track) | 187 | **6,917.702** | 3,556.224 |

(Exact numbers, both libraries, both from the same RTL source and the same
`tt_025C_3v30` corner — see the two synthesis records for the full cell
census.)

`layout/adc-top/README.md`'s as-drawn table gives the SAR-logic reserved
region (incl. its guard ring) as **7,624 µm²**, against §4.2's own original
estimate of 1,000–5,000 µm². Both libraries' *cell* area is a lower bound on
what real place-and-route will need — routed standard-cell area is
routinely 20–40% over raw cell area once cell-to-cell routing, pin access
and any required whitespace are counted, and neither of those numbers is
available yet (place-and-route is follow-on (b), #274, not this issue). Against
that reserved footprint:

- `mcu9t5v0` leaves only 706 µm² of headroom (6,918 of 7,624) before any
  routing overhead at all — comfortably inside a 10–15% routing overhead
  budget, but not inside anything larger, and *already* above §4.2's whole
  original 1,000–5,000 µm² estimate range.
- `mcu7t5v0` leaves 2,663 µm² (35% of the reserved region) before routing
  overhead — enough margin to absorb a real P&R run's overhead and still sit
  near the top of §4.2's original estimate range, not near or past it.

**Choice: `gf180mcu_fd_sc_mcu7t5v0`.** It is smaller on every axis measured
here (28% fewer µm², 6 fewer cell instances, 19% less sequential area) with
no equivalence, mapping, or warning difference between the two runs, and its
margin against the already-drawn reserved footprint is the difference
between "P&R has room to work" and "P&R has almost none" once real routing
overhead is counted. This is a density-vs-reserved-footprint choice, exactly
the kind DR-0023 deferred to this issue — it is not a re-ratification of
DR-0023 itself (DR-0023's own decision, adopting *both* libraries as the
legal set for the digital rail, is untouched), and it is not an area-budget
decision under DR-0024 (pending ratification, out of scope here) either: it
only picks a member of the set DR-0023 already legalized. Both netlists
stay committed under `../flow/sar_ctrl/netlist/` regardless of this choice,
so the un-chosen library's evidence is not thrown away — a later P&R
attempt against `mcu9t5v0` remains possible and auditable if the reserved
footprint itself is ever revisited (DR-0024).

When the gate-level SPICE functional/timing replay (#273) is built, it should run against this chosen library's netlist only —
running it against both would double the replay cost for a library this
issue is not recommending, and the equivalence proof above already
establishes both netlists are functionally identical to the same RTL, so a
functional divergence between the two synthesized netlists is not a
possibility either replay could newly discover.

## Real hardware note: seeding the ring at power-up

The rung-1 XSPICE model seeds its one-hot ring with an initial condition
(`ic=1` on `ph15`'s flip-flop) that has no synthesizable Verilog counterpart
and no counterpart in a real standard-cell flip-flop either (`dffq_1` carries
no reset pin at all — see the cell census in either synthesis record).
`sar_ctrl.v`'s `ph` register therefore powers up in `x` (simulation) or an
arbitrary, unknown state (silicon) rather than the ring's legal one-hot
state. **Real hardware must assert `start` for at least one clock after
power-up** before relying on any other output of this module — asserting
`start` forces `ph[0]` high and every other `ph` bit low on the very next
clock edge regardless of the register's power-up state (`sar_ctrl.v`'s own
`always` block: `ph[0] <= start | ph[15]`, `ph[k] <= ph[k-1] & ~start` for
every other `k`), which re-establishes the one-hot invariant unconditionally.
This is a real hardware requirement the ideal rung-1 model's `ic=1` masked,
and it is why the two upstream ideal decks (`sim/sar-logic-functional/`,
`sim/sar-logic-timing/`) never drive their `_start` net above 0: their
`sar_ctrl_a` never carries this gap in the first place. The follow-on
gate-level replay decks will need to assert `start` for one clock at the top
of every run for exactly this reason — noted here so that follow-on work
does not have to re-derive it.
