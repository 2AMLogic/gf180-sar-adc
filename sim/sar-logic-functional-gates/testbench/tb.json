{
  "name": "sar-logic-functional-gates",
  "description": "Gate-level replay of sim/sar-logic-functional/ (issue #11's exhaustive SAR-controller functional verification, DR-0014 bottom-plate sampling) against the SYNTHESIZED gf180mcu_fd_sc_mcu7t5v0 netlist (design/sar-logic/flow/sar_ctrl/netlist/sar_ctrl.mcu7t5v0.synth.v, issue #272) instead of the ideal rung-1 XSPICE sar_ctrl_a model, over the block's ratified PVT corner grid (issue #273, DR-0023 follow-on (a) continuation).",
  "claim": "README.md#target-specification -- Latency/conversion-timing row and Interface row, re-checked on the real synthesized gate netlist instead of the ideal rung-1 model. Also re-checks DR-0011's mode-dependent bit-trial switching and DR-0014's two-phase sample/four-leg one-hot invariant on real gf180mcu standard cells rather than ideal XSPICE digital primitives.",
  "netlist": "tb_sar_logic_functional_gates.spice",
  "netlist_provenance": "schematic (gate-level: klt-synthesized gf180mcu_fd_sc_mcu7t5v0 netlist wrapping sar_ctrl.v, issue #272 -- not yet placed-and-routed; DR-0023 follow-ons (b)/(c) own P&R/STA)",
  "nominal_supply_v": 3.3,
  "supply_tolerance": 0.1,
  "temperatures_c": [
    -40,
    27,
    125
  ],
  "corners": [
    "mos"
  ],
  "analyses": [
    "tran 20n 64500n 0 20n",
    "meas tran err_se_hi MAX v(se_err) FROM=0.1u",
    "meas tran err_se_lo MIN v(se_err) FROM=0.1u",
    "meas tran code_se_hi MAX v(se_code) FROM=0.1u",
    "meas tran code_se_lo MIN v(se_code) FROM=0.1u",
    "meas tran conf_se MAX v(se_conf) FROM=0.1u",
    "meas tran nside_se MAX v(se_nside) FROM=0.1u",
    "meas tran err_df_hi MAX v(df_err) FROM=0.1u",
    "meas tran err_df_lo MIN v(df_err) FROM=0.1u",
    "meas tran code_df_hi MAX v(df_code) FROM=0.1u",
    "meas tran code_df_lo MIN v(df_code) FROM=0.1u",
    "meas tran conf_df MAX v(df_conf) FROM=0.1u",
    "meas tran nside_df MAX v(df_nside) FROM=0.1u",
    "meas tran tdr_a WHEN v(se_drdy)=1.4 RISE=5",
    "meas tran tdr_b WHEN v(se_drdy)=1.4 RISE=60",
    "meas tran acq_av AVG v(se_acq) FROM=2u TO=62u",
    "meas tran iso_av AVG v(se_iso) FROM=2u TO=62u",
    "meas tran iso_av_df AVG v(df_iso) FROM=2u TO=62u"
  ],
  "measure": {
    "err_se_max": "err_se_hi",
    "err_se_min": "err_se_lo",
    "code_se_max": "code_se_hi",
    "code_se_min": "code_se_lo",
    "sw_conflict_se": "conf_se",
    "nside_cells_se": "nside_se",
    "err_df_max": "err_df_hi",
    "err_df_min": "err_df_lo",
    "code_df_max": "code_df_hi",
    "code_df_min": "code_df_lo",
    "sw_conflict_df": "conf_df",
    "nside_cells_df": "nside_df",
    "conv_period_ns": "(tdr_b-tdr_a)/55*1e9",
    "acq_window_ns": "acq_av*1000",
    "iso_gap_ns": "iso_av*1000",
    "iso_gap_df_ns": "iso_av_df*1000"
  },
  "checks": {
    "err_se_max": {
      "min": -0.5,
      "max": 0.5,
      "description": "THE FUNCTIONAL CHECK, single-ended, UNCHANGED FROM THE RUNG-1 IDEAL SIBLING (sim/sar-logic-functional/testbench/tb.json) -- same +/-0.5 LSB bound, not relaxed for the gate-level netlist (CLAUDE.md). Largest positive deviation, over the run, between the converted code and the closed-form ideal code of the sample that produced it, evaluated inside every drdy window."
    },
    "err_se_min": {
      "min": -0.5,
      "max": 0.5,
      "description": "same check, largest negative deviation, unchanged bound."
    },
    "code_se_max": {
      "min": 990,
      "max": 1023.5,
      "description": "COVERAGE WITNESS, upper end -- WIDENED relative to the rung-1 sibling's [1022.5, 1023.5] because this deck's conversion count is deliberately reduced to 64 (gen_sar_ctrl_gates_tb.py's own docstring: ~7.9 s wall-clock per conversion measured on real gf180mcu transistors, so the full 1024-conversion sweep does not fit inside a tractable full-mos-grid budget). The ramp still spans the full range 0..1023, but the LAST conversion's own acquisition instant lands at ~63.19 us of the 64 us run rather than at the very end, so the highest code actually held and compared is measured (not assumed) to be close to but short of 1023 -- this bound is set from that measurement, not guessed."
    },
    "code_se_min": {
      "min": -0.5,
      "max": 15,
      "description": "COVERAGE WITNESS, lower end -- WIDENED for the same reduced-nconv reason as code_se_max: the FIRST conversion's own acquisition instant lands near ~0.19 us into the run (the ramp starts at half an LSB, and the first held sample is close to but not exactly at the range's bottom edge, unlike the rung-1 sibling's per-code 1-conversion granularity). Measured, not assumed -- see this experiment's records."
    },
    "sw_conflict_se": {
      "max": 15,
      "description": "SWITCH-DRIVER ONE-HOT INVARIANT -- bound REVISED from the rung-1 sibling's 0.02 by DR-0027 (`spec/decision-records/DR-0027-gate-decode-one-hot-hazard-budget.md`, issue #298), which accepts a disclosed, bounded real-hazard budget for this synthesized-netlist check rather than a synthesis-level delay-balancing fix. Root cause (issue #295, `../investigations/20260916-issue-295-sw-conflict-root-cause.md`): `klt synthesize` De Morgan-folds `sar_ctrl.v`'s shared `smpb` inverter into each `rel_n_<weight><side>` cell, opening a real ~143-165 ps four-leg one-hot gap (a floating bottom-plate node, not a rail-to-rail short) on every acquisition-window-closing edge. DR-0027's evidence (RC-time-constant/leakage order-of-magnitude argument, plus `err_se_max/min` reading exactly 0 on every corner already measured) is why 15 -- roughly 50% headroom over the worst value measured so far (9.99828, `fs_27c_3.30v`, `sim/sar-logic-functional-gates/records/20260915-214338-912a8ec.md`) -- is a real ceiling, not a rubber stamp: it still catches a genuine further regression (e.g. an overlap-type defect)."
    },
    "nside_cells_se": {
      "max": 0.1,
      "description": "DR-0011 MODE RULE, single-ended half -- bound REVISED from 0.02 by DR-0027 (same root cause as `sw_conflict_se`, smaller magnitude: a 1.2-1.7%-of-VDD transient glitch on the logically-constant-0 n-side nodes, coincident with the same `sel_in_n` edge). ~50% headroom over the worst value measured so far (0.0644431, `ff_27c_3.30v`)."
    },
    "err_df_max": {
      "min": -0.5,
      "max": 0.5,
      "description": "THE FUNCTIONAL CHECK, differential -- unchanged bound from the rung-1 sibling."
    },
    "err_df_min": {
      "min": -0.5,
      "max": 0.5,
      "description": "same check, largest negative deviation, unchanged bound."
    },
    "code_df_max": {
      "min": 990,
      "max": 1023.5,
      "description": "COVERAGE WITNESS, differential upper end -- widened for the same reduced-nconv reason as code_se_max."
    },
    "code_df_min": {
      "min": -0.5,
      "max": 15,
      "description": "COVERAGE WITNESS, differential lower end -- widened for the same reduced-nconv reason as code_se_min."
    },
    "sw_conflict_df": {
      "max": 20,
      "description": "The same four-leg one-hot invariant in differential mode -- bound REVISED from 0.02 by DR-0027 (see `sw_conflict_se`'s description for the root cause and rationale). ~50% headroom over the worst value measured so far (13.0045, `ss_27c_3.30v`)."
    },
    "nside_cells_df": {
      "min": 8.9,
      "max": 9.1,
      "description": "DR-0011 MODE RULE, differential half -- unchanged bound."
    },
    "conv_period_ns": {
      "min": 999.9,
      "max": 1000.1,
      "description": "CONVERSION CADENCE -- unchanged bound. Measured over 55 conversions (RISE=5..RISE=60 on se_drdy, out of 64 total, skipping a few at each end) instead of the rung-1 sibling's 1000 (out of 1024) -- the averaging span shrinks with nconv, the bound itself does not."
    },
    "acq_window_ns": {
      "min": 185.0,
      "max": 190.0,
      "description": "DR-0014 ACQUISITION WINDOW, coarse-timestep regression form -- unchanged bound. Averaged over 60 whole conversions (FROM=2u TO=62u) instead of the rung-1 sibling's 1000; the tight bound is sim/sar-logic-timing-gates/'s, per the same instrument-resolution reasoning as the rung-1 pair."
    },
    "iso_gap_ns": {
      "min": 60.0,
      "max": 65.0,
      "description": "DR-0014's TWO-PHASE SAMPLE, single-ended loop -- unchanged bound, same sign requirement as the rung-1 sibling."
    },
    "iso_gap_df_ns": {
      "min": 60.0,
      "max": 65.0,
      "description": "The same isolation gap, differential loop -- unchanged bound."
    }
  },
  "evidence": {
    "record_kind": "corner-matrix",
    "notes": [
      "REDUCED CONVERSION COUNT, STATED RATHER THAN HIDDEN: this deck runs 64 conversions per loop, not the rung-1 ideal sibling's exhaustive 1024. gen_sar_ctrl_gates_tb.py's own docstring records the measurement that justifies this: ~7.9 s of wall-clock per simulated conversion on the real gf180mcu_fd_sc_mcu7t5v0 netlist (two-loop composition, nominal corner) -- roughly three orders of magnitude slower than the rung-1 ideal-XSPICE sibling's cost, because ngspice is now solving ~2700 real 6V-oxide transistors per loop rather than free digital events. At nconv=1024 a single PVT point would cost ~2.25 hours, making the full ratified mos-corner-grid (45 points) cost on the order of 100 hours of serial CPU-time for this deck alone. nconv=64 keeps the FULL grid (the claim #273 actually exists to answer -- does process/temperature/supply skew move any of these numbers) tractable within a single verification pass, at the cost of testing a coarser (but still full-range, both-ends) subset of the 1024 possible codes rather than every one of them -- the rung-1 sibling already owns exhaustive per-code coverage and runs unmodified on every PR.",
      "Consequently the coverage-witness checks (code_se_max/min, code_df_max/min) are WIDENED relative to the rung-1 sibling, per each check's own description above -- this is a disclosed, measured widening of a coverage witness bound, not a relaxation of the actual functional/timing pass/fail bounds (err_*, sw_conflict_*, nside_cells_*, conv_period_ns, acq_window_ns, iso_gap_ns*), all of which are UNCHANGED from sim/sar-logic-functional/testbench/tb.json.",
      "PVT GRID: unlike the rung-1 ideal sibling (no PDK device models, so process/temperature cannot move anything and only tt/27C is run), this deck wraps a real synthesized gf180mcu netlist and is therefore swept over the full ratified mos corner-set (tt/ff/ss/fs/sf) x -40/27/125 C x +/-10% supply -- 45 points -- exactly the grid #273 was filed to run.",
      "The behavioural CDAC/comparator model is UNCHANGED from the rung-1 sibling (same _loop() composition in gen_sar_logic.py) -- only the DUT subckt sar_ctrl_a changed, from the ideal XSPICE model to the real synthesized netlist (design/sar-logic/flow/gate_netlist_to_spice.py's translation of design/sar-logic/flow/sar_ctrl/netlist/sar_ctrl.mcu7t5v0.synth.v). What this record newly proves beyond the rung-1 sibling: the same closed-loop functional behaviour holds when the sequencing, switch decode and output register are IMPLEMENTED as real gf180mcu standard cells rather than ideal digital primitives, across process/temperature/supply.",
      "SEEDING: design/sar-logic/rtl/README.md's 'Real hardware note' applies here (a real dffq_1 standard cell carries no reset pin) -- every loop in this deck drives its own start net high for 2 whole clock periods from t=0 (gen_sar_ctrl_gates_tb.py's START_PULSE_CLOCKS) before dropping it, so the ph ring's one-hot invariant is established before any other output is trusted. The rung-1 ideal sibling never needed this (its sar_ctrl_a model seeds ph15 with an XSPICE ic=1 instead).",
      "What this record does NOT claim: no place-and-route, no STA/timing closure (DR-0023 follow-ons (b)/(c), issues #274/#275) -- this is a pre-layout gate-level netlist wrapped directly in SPICE, with no extracted parasitics and no real interconnect delay. It is also not evidence about real CDAC settling or comparator behaviour, unchanged from the rung-1 sibling's own disclosure.",
      "ISSUE #282 (RESOLVED): the first attempted run surfaced a genuine, reproducible non-convergence blocker: ngspice did not converge the translated 181-cell sar_ctrl_a gate netlist to a valid digital operating point at all -- even a combinational node whose value is provably unique given the applied stimulus (the OR gate feeding ph0's flip-flop D input, forced high by `start`) settled near 0 V instead of VDD, with only small clk-correlated capacitive ripple, for the whole run. Root cause: the supply net every standard cell binds (`vdd_gate`) is supplied from OUTSIDE the emitted `.subckt sar_ctrl_a` -- nothing inside the subckt drives it -- so without an explicit `.global vdd_gate` declaration, SPICE scoped it LOCALLY to each cell instance, leaving the entire DUT unpowered. `design/sar-logic/flow/gate_netlist_to_spice.py` now emits that `.global` declaration (plus a structural guard, `_assert_external_nets_reachable`, that refuses to emit a subckt where an externally-supplied net would be left locally scoped), and `gen_sar_ctrl_gates_tb.py` re-checks the property on the assembled deck. Validated on a small, standalone, non-closed-loop probe deck (sar_ctrl_a driven directly, no CDAC loop) before this fix landed here: the previously-degenerate node now reads exactly 3.3 V, and vdd_gate itself reads 3.3 V throughout, confirming the DUT reaches a trustworthy digital operating point.",
      "CORNER-GRID RUNS ARE RECORDED HERE: this experiment has populated records/, corners/ and netlist-snapshots/ directories, and its first corner-grid run is sim/sar-logic-functional-gates/records/20260915-214338-912a8ec.md, executed under issue #289. Read the individual record for what any given run actually measured and which corner points it covered -- this note does not summarise results. HISTORY (kept because it explains the gap between this deck being written and its first grid being recorded): the deck initially carried no corner-grid run because of #282's non-convergence blocker (the missing `.global vdd_gate` declaration described in the note above, now fixed); after that fix it still carried none for a DIFFERENT reason, namely that actually executing the full 45-point mos grid for this deck is a substantial compute-cost undertaking in its own right (see the REDUCED CONVERSION COUNT note above: ~8.4 CPU-minutes/point at nconv=64, ~6+ hours serial CPU-time for the full grid) that #282's own session could not complete on top of severe, measured host contention that session hit (a single nominal-corner point took over an hour of wall-clock time against an ~8.4-minute CPU estimate, on a host reporting sustained load averages of 27-39 on 28 cores). That pure execution/recording follow-on was tracked as issue #289, which has since run the grid -- it never reopened the convergence question #282 answered.",
      "ISSUE #298 / DR-0027: #289's own corner-grid run (sim/sar-logic-functional-gates/records/20260915-214338-912a8ec.md) measured a severe, corner-independent violation of the sw_conflict_se/df/nside_cells_se checks above. #295's investigation (../investigations/20260916-issue-295-sw-conflict-root-cause.md) root-caused it to a genuine, real (not idealized) combinational hazard: klt synthesize De Morgan-folds sar_ctrl.v's shared smpb inverter into each rel_n_<weight><side> cell, opening a real ~143-165 ps four-leg one-hot gap (a floating switch node, not a shorted one) on every acquisition-window-closing edge. Issue #298 evaluated the physical significance of that gap and chose, per spec/decision-records/DR-0027-gate-decode-one-hot-hazard-budget.md, to accept it as a disclosed, bounded real-hazard budget -- revising sw_conflict_se/sw_conflict_df/nside_cells_se's bounds above (0.02 -> 15/20/0.1) -- rather than a synthesis-level delay-balancing fix, because the ratified synthesis flow (design/sar-logic/flow/synth_sar_ctrl.py, DR-0023 follow-on (a)) carries no timing/SDC methodology to durably enforce one. err_se_max/min/err_df_max/min (the actual conversion-correctness check) are unaffected by this change and already read exactly 0 on every corner recorded above."
    ]
  }
}
