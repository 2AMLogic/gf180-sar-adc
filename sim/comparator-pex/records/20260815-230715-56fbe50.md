# Record 20260815-230715-56fbe50

- **Record ID**: 20260815-230715-56fbe50
- **Claim**: issue #173 (T1 checklist item 7, "Post-layout verification") -- a genuine `klt pex` run against this repo's post-layout `COMPARATOR` block, re-grading item 7 from a live tool report instead of "N/A by construction". Not itself a spec-line performance claim (the run below errors before any measurement is obtained -- see Result).
- **Netlist provenance**: extracted (`klt pex` / `klt extract --parasitics`)
- **Corner matrix run**: 1 corner (`typical/novdd/27C`, nominal supply/temperature, no PVT sweep) -- see `sim/comparator-pex/testbench/request.json`'s own `_comment`. Subset justification (sim/README.md): the failure below is structural (a subcircuit pin-count mismatch at netlist elaboration, before any corner-dependent analysis runs) and reproduces identically at every corner; expanding to the full PVT grid would not surface additional information. Re-run at the full grid once the blocker below is resolved.
- **Toolchain**: klt `klt 0.2.0` at commit `755d3eff3cebd2df9e874e446670a202f61ffe1d` -- a SEPARATE, investigative pin from `layout/toolchain.json`'s production one; see `layout/adc-top/parasitics/run_pex_comparator.py`'s module docstring for why the production pin was not bumped in the same change, and gf180-sar-adc#178 for the deferred bump.
- **PDK**: `gf180mcuD` (open_pdks c6d73a35f524070e85faff4a6a9eef49553ebc2b), resolved via `klt pdk find`, root `/Users/rwalters/.volare`.
- **Repo git sha**: `56fbe5035e13c1cff9cbb279e65e7ac1f087f24a`

## Reproduce

```
uv tool install --force git+https://github.com/2AMLogic/klayout-tools@755d3eff3cebd2df9e874e446670a202f61ffe1d
python3 layout/adc-top/parasitics/run_pex_comparator.py --check   # reachability only
python3 layout/adc-top/parasitics/run_pex_comparator.py           # mint a new record
```

## Result

`klt pex` exit code **4**, report `status` **`error`**.

**Item 7 re-grade: FAIL -- blocked, not "N/A by construction".** `klt pex` exists, was installed, and was run for real against this repo's post-layout `COMPARATOR` GDS with a genuine `klt sim`-format testbench. It failed structurally, before any PVT-dependent measurement, because `klt pex`'s single-`.include` DUT-swap mechanism requires the schematic and extracted-side netlists to define the SAME top-level `.subckt` name and pin list. They do not, for any block `klt extract` has produced in this repo: the extracted `COMPARATOR` interface is `clk dout doutb ibias pon pop vdd vinn vinp vss vsubs` (11 pins -- exposes the preamp's internal `pon`/`pop` output nodes and the `vsubs` body-tap net), while the hand-written schematic `comparator` interface is `vinp vinn clk ibias dout doutb vdd vss` (8 pins -- neither exists at that granularity in a physically-unmodelled schematic). The generated extracted-side deck's own `ngspice` diagnostic, copied verbatim below, names this exactly:

```
Note: Compatibility modes selected: hs a

Warning: m=xx on .subckt line will override multiplier m hierarchy!


Circuit: * klt sim -- generated corner deck, do not edit

Too few parameters for subcircuit type "comparator" (instance: xxdut)
    Simulation interrupted due to error!
```

This is the SAME interface mismatch this repo's own #89-era extracted-core testbenches (`gen_extracted_core_tb.py` et al.) solve with a bespoke per-pin `Xdut` wiring generator instead of a `.include` swap -- built specifically because `klt extract`'s output routinely exposes body/tap/internal nets a hand-written schematic subckt does not. `klt pex` has no equivalent caller-supplied pin-remapping mechanism; filed generically as friction against `klayout-tools`: https://github.com/2AMLogic/klayout-tools/issues/1030 . The deferred `layout/toolchain.json` production-pin bump this investigation also motivated is tracked separately: gf180-sar-adc#178.

## Artifacts in this record

- `netlist-snapshots/20260815-230715-56fbe50.spice` -- the extracted netlist `klt pex` wrote (extraction itself succeeded; only the re-sim against it failed)
- `corners/20260815-230715-56fbe50/pex-report.json` -- the raw `klt pex` JSON report (native format)
- `corners/20260815-230715-56fbe50/schematic.log` -- schematic-side `klt sim` ngspice log
- `corners/20260815-230715-56fbe50/extracted.log` -- extracted-side `klt sim` ngspice log (the diagnostic quoted above)

Append-only per `sim/README.md`'s evidence rule: this record is never overwritten. A later run (once the interface-mismatch blocker above is resolved, upstream or by this repo authoring a pin-matched schematic wrapper) mints a new `<record-id>` beside it.
