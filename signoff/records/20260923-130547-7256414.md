# T1 verdict re-graded after the `V_CM` row became a measured full-PVT result — every row unchanged

**Record id:** `20260923-130547-7256414` (minted on `feature/issue-358`,
rebased onto `origin/main` @ `96a8546`)
**Issue:** #358
**Supersedes:** [`20260923-114030-904af96`](20260923-114030-904af96.md)
**Report:** [`../reports/20260923-130547-7256414/signoff.json`](../reports/20260923-130547-7256414/signoff.json)
(machine-readable, byte-identical to the grader's `--format json` output) and
[`signoff.txt`](../reports/20260923-130547-7256414/signoff.txt) (the same run's
text rendering, ANSI stripped — klayout-tools#2227).
**Manifest:** [`../gf180-sar-adc.manifest.json`](../gf180-sar-adc.manifest.json)
**Grader:** `klt 0.5.0+g67d617f899c7`
(`67d617f899c7fb941b86cfc85491f75b91ae1176`) — unchanged;
`signoff/toolchain.json`'s pin is untouched by this issue. Installed into a
throwaway virtualenv from the pinned commit and passed via `--klt`, because
this host's plain `klt 0.5.0` grades a **ten**-item checklist and would render
20 rows where the pin renders 22 (`signoff/toolchain.json`'s `_comment`).

## Why this record exists

**The document behind T1 item 8 changed again.** Issue #358 — DR-0026's named
follow-up — re-ran the decks that own ratified spec rows against a real
external `V_cm` drive network at DR-0026's derived budget
(`Z_vcm = 220 Ω`, `C_dec = 40 nF`), over each deck's own governing PVT grid,
as paired same-commit arms. `sim/characterization-summary.md`'s `V_CM` row now
carries that measured result instead of the one-temperature / one-supply
exploratory sensitivity sweep it previously cited, and the follow-up list that
named #358 as an open gap was updated with the outcome.

`signoff/run_signoff.py --check` re-hashes the wrapped document on every pull
request and fails loudly on exactly that, which is why this is a re-mint and
not an edit.

**This record also supersedes an un-merged one.** An earlier re-mint on this
branch (`20260923-122153-4334017`) graded the pre-rebase tree. That tree
never existed on `main`: PR #390 (issue #381) landed its re-extraction and its
own item-8 re-pin first, so the branch was rebased onto `96a8546` and the
earlier report was dropped rather than superseded in place — it graded a
`content_hash` that no commit in this repository's history carries. This
record grades the merged tree.

## What moved in the verdict

```
block: gf180-sar-adc  kind: mixed-signal
tier: none
T1: 7/22 items met        (unchanged)
```

**No row changed.** Every entry in `items[]` is identical to the superseded
record, field for field; the only difference anywhere in the report is the
item-8 `content_hash` the grader echoes back:

```
sha256:0f1bcf26…c720c0   ->   sha256:7ff4e618…1c22e2b
```

Checked mechanically, not by reading:

```python
rows = lambda d: {(i["id"], i.get("partition", "")): (i["status"], i.get("reason"))
                  for i in d["items"]}
rows(old) == rows(new)                     # True
old["t1_met_count"], new["t1_met_count"]   # (7, 7)
old["tier"], new["tier"]                   # (None, None)
```

That null is the intended outcome and worth stating as a claim. T1 item 8 asks
whether a current, aggregated characterization report **exists and has been
re-read** — not whether the design passes. This pass re-derived one of that
report's rows from a full-PVT campaign and moved no verdict in either
direction; ENOB, SFDR and Area still FAIL, at the same corners, and the
`INL / DNL` row still PASSes its ratified `< 1 LSB` window under the `V_cm`
network, which is what the campaign set out to establish.

## What this record does NOT assert

- **Not a new full re-read of `sim/characterization-summary.md`.** Both item-8
  envelopes keep `assertion.reread_commit: 93ddfe3` (2026-09-21) and gained a
  second `assertion.partial_updates[]` entry — beside #381's — naming exactly
  what #358 re-derived and what it did not. Bumping the re-read stamp without
  redoing the row-by-row re-read against `README.md` would be the defect that
  document's own §"Currency defects" item 2 records.
- **Not that the `V_cm` result covers the current governing extraction.** The
  campaign's **extracted** arms ran on the pre-#381 extraction vintage, which
  PR #390 superseded while #358 was in review. The paired delta stands on the
  vintage it was measured on, and the re-extraction moved the ideal arm's
  worst `DNL` *away* from the bound (0.727556 → 0.681240 LSB) so the committed
  headroom figure is the pessimistic one — but the delta on the current
  extraction is **unmeasured**. Filed as **#392**, named in both envelopes'
  `known_gaps_carried`, in the wrapped document's Freshness section, and in
  `sim/vcm-full-pvt/README.md`.
- **Not that the campaign is complete.** `ENOB`/`SFDR` and the extracted power
  deck were never re-run against the network. Both are wired into
  `sim/vcm-full-pvt/run_full_pvt.sh` and the reasoning is recorded there.
- **The DR-0036 currency gap #381 declared rides along unchanged** — PR #387
  rewrote `README.md#target-specification`'s **Supply** row after the `93ddfe3`
  re-read and the wrapped document has not caught up. That is DR-0036's own
  work, carried here rather than silently dropped.
- **No manifest row was re-pointed, no `klt` pin moved, and no ratified bound,
  target or `tb.json` check was touched by the work this grades.** The only
  manifest edit is the item-8 `content_hash` pin, which tracks the wrapped
  document's sha256 by design.

## Reproduce

```bash
python3 signoff/run_signoff.py --check     # stdlib only; re-derives every input
python3 signoff/run_signoff.py --selftest  # 10 tampered inputs, all caught
python3 signoff/run_signoff.py --regen --klt <pinned 0.5.0+g67d617f899c7 build>
```

Append-only per `signoff/README.md`: this record is never edited. A later
re-grade mints a new `<record-id>` beside it and points back here via
**Supersedes**.
