{
  "name": "loom-workspace",
  "description": "Loom-managed workspace with AI development orchestration",
  "private": true,
  "scripts": {
    "worktree:return": "./.loom/scripts/worktree-return.sh",
    "check:ci": "npm run --silent test && npm run --silent check:syntax",
    "check:all": "bash sim/selftest.sh",
    "check:syntax": "bash -n sim/selftest.sh && bash -n sim/env.sh && bash -n sim/smoke_test/run_smoke_test.sh && python3 -m compileall -q sim",
    "test": "python3 -m unittest discover -s sim/tests -t sim/tests"
  }
}
