# Record 20260812-132011-f613571

- **Record ID**: 20260812-132011-f613571
- **Claim**: `README.md#target-specification` — INL / DNL, < 1 LSB baseline
  (ratified) / < 0.5 LSB stretch (aspirational), untrimmed, 3σ Monte Carlo
  mismatch. This is **not a new mismatch campaign** — it re-derives the same
  claim record 20260801-093800-c033611 already substantiates, this time
  through [`klt yield`](https://github.com/2AMLogic/klayout-tools/blob/main/docs/cli/yield.md)
  (2AMLogic/klayout-tools, epic #710), the generic statistical-yield tool,
  rather than this experiment's own bespoke script — producing the
  machine-checkable, tool-independent T1-row evidence
  [`docs/design-evidence-tiers.md`](https://github.com/2AMLogic/klayout-tools/blob/main/docs/design-evidence-tiers.md)
  item 6 ("Statistical claims carry Monte Carlo evidence") asks for, plus
  the two checks that record did not yet have: a deliberate, seeded
  **negative control** and an **analytic cross-check**, both of which
  `klt yield` enforces natively. See
  [2AMLogic/klayout-tools#818](https://github.com/2AMLogic/klayout-tools/issues/818)
  (Phase 1c of epic #710) for the upstream tracking issue.
- **Netlist provenance**: none of its own — this record reformats and
  re-analyses record 20260801-093800-c033611's own real per-trial data
  (`sim/mc-cdac-mismatch/runs/20260801-093800-c033611/trials_n20000.csv`,
  behavioral CDAC mismatch model, N=20000, seed=20260801) plus one new real
  simulation (the negative control below). No claim value in this record is
  invented or re-derived analytically from scratch — every sample fed to
  `klt yield` is a real, re-runnable simulation output.
- **Corner matrix run**: nominal only (unchanged from the source record —
  see its own "Corner matrix run" / "Subset-corner justification" for why a
  device-mismatch statistical claim is evaluated at nominal PVT, not swept).
  **This record's own gap, stated plainly per
  `docs/design-evidence-tiers.md`'s "Coverage honesty" rule**: item 6 asks
  for MC results "combined with (not instead of) process corners" — this
  record, like its source, is the mismatch leg only; the process-corner leg
  is `sim/adc-inl-dnl/`'s job (a separate, already-populated experiment
  directory) and combining the two into one verdict is not done here.
- **Statistical convention**: unchanged from the source record for the
  nominal draw (`N = 20000`, `seed = 20260801`, `sigma_u = 0.7372097807744856
  %`, the chosen design's calibrated Pelgrom coefficient). This record adds:
  - **Negative control** (new real simulation, not reused from any prior
    record): the CDAC unit-cap mismatch sigma forced to **3x** the
    calibrated design value — `sigma_u = 2.211629342323457 %` — via
    `sim/mc-cdac-mismatch/testbench/mc_cdac_mismatch.py --sigma-u
    2.211629342323457 --trials 2000 --seed 99109901`, the exact same tool
    and methodology as the nominal campaign (same behavioral model, same
    code-256 DNL/INL extraction), just a deliberately worse `sigma_u`. Raw
    output: `sim/mc-cdac-mismatch/runs/20260812-132011-f613571/`
    (`trials_n2000_3xsigma.csv`, `summary_n2000_3xsigma.json`).
  - **Analytic cross-check**: `klt yield`'s `mismatch_offset` model, fed
    `sigma = analytic_sigma_dnl_lsb` / `analytic_sigma_inl_lsb` — the exact
    closed-form Pelgrom-law predictions (`sqrt(511)*sigma_u` /
    `sqrt(512)/2*sigma_u`) `mc_cdac_mismatch.py` already computes and the
    source record's own "Result" table already cites, not re-derived here.
- **Result**:

  `klt yield mc-samples.json --limits spec-limits.json` (both files in this
  directory, generated by `build_yield_evidence.py`, real per-trial samples
  and real spec limits, no synthetic data):

  | Measurement | Limits (LSB) | Status | Empirical yield | `sigma_to_spec` | Negative control | Analytic cross-check |
  |---|---|---|---|---|---|---|
  | `dnl_at_256_lsb_baseline` | ±1.0 | **pass** | ≥ 0.999816 (95% CI) | 5.975 | **detected** (yield 0.9485 [0.9379, 0.9578], N=2000) | consistent (empirical σ=0.167268 vs analytic σ=0.166649, Δ +0.37%) |
  | `dnl_at_256_lsb_stretch` | ±0.5 | fail | 0.997200 [0.996365, 0.997884] | 2.986 | **detected** (yield 0.6940 [0.6733, 0.7142], N=2000) | consistent (same σ pair as above) |
  | `inl_at_256_lsb_baseline` | ±1.0 | **pass** | ≥ 0.999816 (95% CI) | 11.954 | **not_detected** (yield 1.0 [0.9982, 1.0], N=2000 — see note) | consistent (empirical σ=0.0836329 vs analytic σ=0.0834058, Δ +0.27%) |
  | `inl_at_256_lsb_stretch` | ±0.5 | **pass** | ≥ 0.999816 (95% CI) | 5.975 | **detected** (yield 0.9485 [0.9379, 0.9578], N=2000) | consistent (same σ pair as above) |

  Full machine-readable output: `klt-yield-report.json` (schema_version 1,
  `status: "fail"` — the stretch DNL row misses its target); human-readable:
  `klt-yield-report.txt`. Command exit status: `3` ("ran successfully, at
  least one measurement's yield claim was not supported at the stated
  confidence" — `docs/cli/yield.md#exit-codes`).

  **Cross-validated against the source record's own bespoke-script numbers**:
  `sigma_to_spec` above (5.975 DNL / 11.954 INL at the ±1 LSB baseline) is
  `klt yield`'s Cpk-derived sigma-margin, computed by an entirely independent
  code path from the source record's own `sigma_at_spec` (5.978 DNL / 11.957
  INL, `summary_n20000.json`) — the two agree to 3 significant figures, an
  independent confirmation that neither tool's arithmetic is wrong. The
  ±0.5 LSB stretch row's near-3σ DNL shortfall (2.986σ here vs. 2.989σ in the
  source record's own N=20000 cross-check) reproduces the same "just short of
  3.0σ, statistically indistinguishable from meeting it" finding the source
  record already reported — **no spec relaxation, reported exactly as
  measured**, consistent with both records' shared `CLAUDE.md` obligation.

  **Negative control, read honestly**: the 3x-sigma defect is detected (a
  statistically distinguishable yield drop) on 3 of the 4 rows, but **not**
  on `inl_at_256_lsb_baseline` — the ±1 LSB baseline window is wide enough
  (≈12σ at the nominal sigma) that even a 3x sigma degradation does not push
  INL's empirical yield below what a 2000-sample negative-control draw can
  distinguish from 1.0 at 95% confidence. This is not a defect in the
  negative control; it is the honest, expected outcome of a loose limit
  against a moderate injected degradation, and is reported as `klt yield`
  itself reports it (per-measurement, not a single pass/fail for the whole
  campaign) — see `docs/cli/yield.md#negative-control`.

- **Links**:
  - Nominal-campaign source data (reused, not re-run):
    `sim/mc-cdac-mismatch/runs/20260801-093800-c033611/trials_n20000.csv`,
    referenced by record `20260801-093800-c033611.md`.
  - Negative-control raw output (new):
    `sim/mc-cdac-mismatch/runs/20260812-132011-f613571/trials_n2000_3xsigma.csv`,
    `sim/mc-cdac-mismatch/runs/20260812-132011-f613571/summary_n2000_3xsigma.json`.
  - `klt yield` evidence bundle (new):
    `sim/mc-cdac-mismatch/yield-evidence/build_yield_evidence.py` (the
    reformatting script — deterministic, re-runnable),
    `sim/mc-cdac-mismatch/yield-evidence/mc-samples.json`,
    `sim/mc-cdac-mismatch/yield-evidence/spec-limits.json`,
    `sim/mc-cdac-mismatch/yield-evidence/klt-yield-report.json`,
    `sim/mc-cdac-mismatch/yield-evidence/klt-yield-report.txt`.
  - Calibration/topology source (unchanged): `spec/cdac-sizing-memo.md`
    §2-§4.
  - Tool: [`klt yield`](https://github.com/2AMLogic/klayout-tools/blob/main/docs/cli/yield.md)
    ([2AMLogic/klayout-tools](https://github.com/2AMLogic/klayout-tools),
    `klt-yield-native==0.1.0`).
- **Timestamp / author**: 2026-08-12T13:20:11Z, loom-builder@2amlogic.com
- **Supersedes**: (none — this is a new, tool-independent re-derivation of
  20260801-093800-c033611's claim, not a correction to it; that record
  stands as the original bespoke-script evidence, cited above, not replaced)

## Environment

Everything needed to re-run this record:

- Negative-control simulation: `sim/mc-cdac-mismatch/testbench/mc_cdac_mismatch.py`
  (stdlib + numpy + scipy, same tool the source record used) —
  `python3 sim/mc-cdac-mismatch/testbench/mc_cdac_mismatch.py --sigma-u
  2.211629342323457 --trials 2000 --seed 99109901 --out-csv
  runs/20260812-132011-f613571/trials_n2000_3xsigma.csv --out-json
  runs/20260812-132011-f613571/summary_n2000_3xsigma.json`
- Evidence-bundle build: `python3
  sim/mc-cdac-mismatch/yield-evidence/build_yield_evidence.py` (pure
  stdlib, reads the CSVs above, writes `mc-samples.json` /
  `spec-limits.json` — deterministic, no RNG of its own).
- `klt yield` invocation (from `sim/mc-cdac-mismatch/yield-evidence/`):
  `klt yield mc-samples.json --limits spec-limits.json --format json` (or
  omit `--format json` for the text report). Requires the
  `klt_yield_native` Rust extension built — see
  [`docs/cli/yield.md#building-the-native-extension`](https://github.com/2AMLogic/klayout-tools/blob/main/docs/cli/yield.md#building-the-native-extension)
  in the klayout-tools repo (`klt-yield-native==0.1.0`, built from
  `2AMLogic/klayout-tools` commit `373181f` / PR #902).
- git: `f613571` on `main` (clean at generation time).

## Consuming this evidence without bespoke parsing

`klt-yield-report.json` is a stock `klt yield --format json` envelope
(`schema_version: 1`) — a future signoff aggregator (`klt signoff
--manifest`, epic #706) can cite it directly, either file-backed or
command-backed, exactly per
[`docs/cli/signoff.md`](https://github.com/2AMLogic/klayout-tools/blob/main/docs/cli/signoff.md#tier-verdict-report---manifest):

```json
"6": {
  "command": ["klt", "yield", "mc-samples.json", "--limits", "spec-limits.json", "--format", "json"],
  "cwd": "sim/mc-cdac-mismatch/yield-evidence/"
}
```

or, to grade against this exact committed report without re-running
anything:

```json
"6": {"file": "sim/mc-cdac-mismatch/yield-evidence/klt-yield-report.json"}
```

No file in this directory needs any gf180-sar-adc-specific parsing — the
envelope shape is entirely generic `klt yield` JSON, the same shape
`klt signoff`'s Phase 2a binding (klayout-tools#870/#880) already
recognises.

---

Append-only per `sim/README.md`: never edit or delete this file — a re-run
or correction mints a new record-id and references this one via
**Supersedes**.
